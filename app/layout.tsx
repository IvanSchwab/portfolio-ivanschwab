// This file is no longer used - it's replaced by app/[locale]/layout.tsx
// Keeping it for backwards compatibility
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
